<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Octopress | My Octopress Blog]]></title>
  <link href="http://svatyvabin.github.io/blog/categories/octopress/atom.xml" rel="self"/>
  <link href="http://svatyvabin.github.io/"/>
  <updated>2015-12-02T20:34:35+08:00</updated>
  <id>http://svatyvabin.github.io/</id>
  <author>
    <name><![CDATA[Your Name]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[在 Github Pages 上使用 Octopress 创建自己的博客]]></title>
    <link href="http://svatyvabin.github.io/blog/2015/12/02/zai-github-pages-shang-shi-yong-octopress-chuang-jian-zi-ji-de-bo-ke/"/>
    <updated>2015-12-02T19:50:37+08:00</updated>
    <id>http://svatyvabin.github.io/blog/2015/12/02/zai-github-pages-shang-shi-yong-octopress-chuang-jian-zi-ji-de-bo-ke</id>
    <content type="html"><![CDATA[<h2>1. <a href="http://octopress.org/docs/setup/">安装 Octopress</a></h2>

<h3>1.1 安装必要的软件</h3>

<p>在终端下输入：
<figure class='code'><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
</pre></td><td class='code'><pre><code class=''><span class='line'>sudo apt-get install ruby ruby-dev nodejs rbenv&lt;/p&gt;
</span><span class='line'>
</span><span class='line'>&lt;p&gt;sudo gem install execjs</span></code></pre></td></tr></table></div></figure></p>

<p>gem 下载速度比较慢，耐心等待，如果提示 <code>Error</code> 或者一直没有响应，可以用淘宝提供的<a href="https://ruby.taobao.org/"> gem 镜像</a>。</p>

<h3>1.2 安装 Octopress</h3>

<p>下载 Octopress，并进入 Octopress 目录：
<code>
git clone git://github.com/imathis/octopress.git octopress
cd octopress
</code></p>

<p>然后安装依赖：
<code>
gem install bundler
rbenv rehash
bundle install
</code>
注意每条命令的提示，确保每一步都是正确的。如果命令出错，应该是缺少某个软件，可以谷歌搜索一下解决。</p>

<p>最后安装默认的主题：
<code>
rake install
</code></p>

<h2>2. 部署到 Github Pages 上</h2>

<h3>2.1 创建 Github Pages</h3>

<p>根据 <a href="https://pages.github.com/">Github Pages</a> 官网的简易教程，就可以创建一个自己的 Github Pages 网站了。
需要注意的是，repository 名称前缀必须是你的用户名，例如你的用户名是 Joe，那你的 repository 名称就必须是 joe.github.io。</p>

<p>创建成功后，为了方便以后使用 git 命令，建议在 <a href="https://github.com/settings/ssh">github</a> 的设置中添加本机的 <code>SSH keys</code>，这样就不需要输入密码了，如何生成 <code>SSH keys</code> 可以参考<a href="https://help.github.com/articles/generating-ssh-keys/#platform-linux">上面的官方教程</a>。</p>

<h3>2.2 部署到 Github Pages</h3>

<p>在 octopress 目录下，使用命令：
<code>
rake setup_github_pages
</code>
之后会要求你输入 repository 地址，在你刚刚创建的 repository 主页上复制类似 <code>git@github.com:joe/joe.github.io.git</code> 的地址就粘贴到这里就行了（使用 Ctrl + Shift + V）。</p>

<p>之后使用命令：
<code>
rake generate
</code>
生成博客。</p>

<p>使用命令：
<code>
rake deploy
</code>
将网站上传到你的 Github Pages 上</p>

<p>同时将 octopress 下的 source 上传到 Github 上：
<code>
git add .
git commit -m "commit"
git push origin source
</code>
注意，这里必须 push 到 <code>source</code> 分支上，<code>source</code> 分支上保存着文章等信息。<code>master</code> 分支会自己调用 <code>source</code> 分支上的信息。</p>

<p>现在就可以访问 <code>joe.github.io</code> 来查看你的博客是否部署成功了。</p>

<h2>3. 写博客</h2>
]]></content>
  </entry>
  
</feed>
